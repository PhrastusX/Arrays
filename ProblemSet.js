// Create an array with the names of different types of fruit and save it to a variable

// Write code to access the third item in the fruit array and save it to a new variable.

// Overwrite the fruit at the last position in the array with a new string. It can be anything you want.

// Be sure to test your code is doing what you think it is doing by console.log() ing throughout.

const newArray = ["banana", "peach", "orange", "apple", "carrot"]

let orange = newArray[2]

newArray.push("mango")

console.log(newArray)
console.log(orange)